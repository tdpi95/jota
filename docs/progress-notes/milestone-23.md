# Milestone 23 notes (Journal editor syntax highlighting)

[← back to PROGRESS.md](../../PROGRESS.md)

- Requested directly by the user ("add markdown preview for journal, live on editor similar to Obsidian") — started ahead of milestone 18's remaining items, same pattern as 19-21. `AskUserQuestion` was used to scope "live preview" before building anything (Obsidian-style hide-the-marks rendering vs. a toggle/split-pane preview vs. VS Code-style source coloring); the user dismissed that question and instead answered a follow-up message directly — "how about color highlight like vscode?" — settling on source-level syntax coloring, explicitly *not* a rendered/WYSIWYG preview. The stored journal body stays exactly what's typed; only its on-screen color changes.
- **`MarkdownEditor.tsx`** (new) wraps CodeMirror 6 directly (`codemirror`, `@codemirror/lang-markdown`, `@codemirror/language`, `@lezer/highlight` added as client deps) rather than a React wrapper package, so theming/sizing stays under this app's own CSS. Same controlled `{value, onChange, placeholder?, disabled?, className?}` contract `MarkdownTextarea` used, so `JournalDayPage`'s existing autosave/dirty-tracking logic needed no changes beyond the import and `onChange`'s signature. Used only for the journal body — task/project descriptions keep the plain `MarkdownTextarea` (renamed comment to clarify the split), since rendering wasn't asked for there and their free-text-continuation-line grammar makes "just markdown" a less clean fit anyway.
- **A real bug caught before landing**: an early version styled `tags.list` directly for the bullet/number marker's color — `@lezer/markdown` maps list nodes to that tag *recursively* (every descendant, not just the marker), so bold/code/links *inside* any list item ended up re-colored/bolded by the list rule on top of their own token's styling. Caught via computed-style verification in the actual running app (not just visual inspection) before it shipped; fixed by dropping the `tags.list` rule entirely — the marker is already covered by `--md-mark` via the (non-recursive) `processingInstruction` tag.
- **Verified** (initial pass): `npx tsc --noEmit` and `npm run build -w client` clean; verified live against the running dev server, headings/bold/italic/strikethrough/inline-code/links/blockquotes each confirmed via computed styles in both light and dark themes.
- **Workspace safety note**: the first live-verification pass in this milestone was accidentally run against the user's real active workspace (`Everything`) rather than a scratch one, and typed test markdown landed in that day's actual journal entry via autosave. Caught immediately and reverted with `git revert` in that vault's own repo (the same undo mechanism the app itself exposes), not a manual file edit. Every verification pass after this one in the milestone switched to the registered `Poco Demo` scratch workspace first (`POST /api/workspaces/:id/open`) and switched back to whatever was active afterward — documented here so a future session doesn't have to rediscover why that dance happens before every live check.

## Follow-up: autosave was creating too many commits

The user reported "journal auto save is too quick. it makes too many history entries" — every autosave is a git commit (PLAN.md's undo mechanism), and the original 800ms debounce fired on almost every normal mid-sentence pause while composing a journal entry. Bumped `AUTOSAVE_DELAY_MS` to 4000ms in `JournalDayPage.tsx`. The debounce mechanism itself (reset on every keystroke) was untouched — continuous typing still never saves mid-word — and the existing unmount-flush-on-navigate effect still guarantees no work is lost regardless of the longer wait.

## Follow-up: monospace font + accent-colored headings

The user asked for a monospace editor font and accent-colored headings. `.journal-body`'s font-family moved from `'Newsreader', Georgia, serif` to a monospace stack; `--md-heading` moved from `--ink` to `--accent`. This required splitting `t.strong` (bold) out of the `HighlightStyle` rule it had been sharing with `heading1`-`heading6` — they'd both used `--md-heading` for convenience, and moving that token to accent without splitting the rule would have accent-colored every bold word too, not just headings.

## Follow-up: embed JetBrains Mono, replace IBM Plex Mono everywhere

Asked to use "JetBrains Mono" for the editor, then (after an interrupted first attempt added it only as a fallback ahead of IBM Plex Mono, which wasn't what was asked) corrected to a straight replacement, scoped to the editor. The user then supplied the actual font zip and asked to embed it and replace IBM Plex Mono **everywhere**, not just the editor.

- Confirmed first that this app doesn't bundle or web-load any of its custom fonts at all (`Newsreader`/`Manrope`/`IBM Plex Mono` were all just named in CSS, no `@font-face`, nothing in `index.html`) — so a plain rename would only render correctly on a machine that happened to have the font installed.
- Extracted the official release zip (SIL Open Font License — confirmed redistribution-safe), took just the four weights/styles this app actually uses (Regular/Bold/Italic/BoldItalic, `.woff2`) into `client/public/fonts/jetbrains-mono/` alongside `OFL.txt`, and added four `@font-face` rules to the top of `app.css`. Vite's `public/` copies verbatim into `client/dist`, which is exactly what both the dev server and the packaged Electron app (`electron-builder.yml`'s `client/dist` extraResource) already serve — no build/packaging changes needed.
- Every remaining `'IBM Plex Mono', monospace` in `app.css` (24 spots — badges, task/journal IDs, commit hashes, tags, timestamps) was replaced with `'JetBrains Mono', monospace`. The `design/` prototype canvas still references IBM Plex Mono in its own `.dc.html` source — left untouched, per its own README, since it's a separate artifact from the running app.
- **Verified**: `npx tsc --noEmit` and the build clean; live against the scratch `Poco Demo` workspace — confirmed the font file actually serves (200) and `document.fonts` reports it `loaded` (not falling back), plus a canvas glyph-width probe confirmed genuine JetBrains Mono metrics rather than a silent fallback to generic monospace.

## Bug: code text rendered black instead of the intended code color

During the two JetBrains Mono edits above, the `t.monospace` `HighlightStyle` rule (which had `color: var(--md-code)` and `backgroundColor: var(--md-code-bg)`) was twice replaced with a rule that set only `fontFamily`, silently dropping the color/background — reported by the user as "use different color for code block, it's now black." Restored `color`/`backgroundColor` (font-family no longer needed its own rule, since the whole editor is monospace via `.journal-body` already).

## Follow-up: full-width code block background, then the bugs that surfaced

Asked to make the code block background span the full editor width rather than just hug the text. A single `HighlightStyle` rule can only paint the characters it matches, so this needed a real decoration: `codeBlockBackground` (a `ViewPlugin` in `MarkdownEditor.tsx`) walks the syntax tree for `FencedCode`/`CodeBlock` nodes and applies a `Decoration.line` background to every line the block spans, with corner-rounding on the first/last line. Verification here was deliberately *not* run live — the user's own dev server (`npm run dev` at the root) was running at the time, and since the REST server's "active workspace" is shared global state (`~/.poco/config.json`) between any process talking to it, switching workspaces to test would have yanked the workspace out from under the user's live session. Relied on `npx tsc --noEmit`/build plus careful reasoning about the CodeMirror APIs involved (a well-documented pattern — `syntaxTree().iterate()` + `RangeSetBuilder` + `Decoration.line`) and asked the user to confirm directly.

Two real bugs surfaced from that choice not to verify live, both reported by the user and both traced correctly on the first pass once reported:

1. **"text selected highlight is behind the code block background and too bright in dark theme."** `.cm-line` (where the new background lived) sits *above* CodeMirror's selection-highlight layer in the DOM (a `.cm-layer` sibling with negative `z-index`, confirmed by reading `@codemirror/view`'s own injected stylesheet) — an opaque line background fully covered the selection underneath. Fix: made `--md-code-bg` translucent (a 6%/8% alpha wash of `--ink`, light/dark) instead of an opaque `--surface-muted` fill, reusing the alpha-token convention `--overlay-backdrop`/`--drag-over-bg` already established. Translucent lets the selection color show through blended instead of erased, and incidentally fixed "too bright in dark theme" too — a solid muted-surface fill read as a bright grey block against the dark theme's near-black page; the translucent wash is much quieter.
2. **"now there are 2 backgrounds, background behind text and background of code block."** The inline-code chip background and the new full-line background were both coming from the same source — `t.monospace`'s `HighlightStyle` rule set a per-character `backgroundColor`, and `@lezer/markdown` tags a fenced block's text and a single-backtick inline span with the exact same `monospace` tag, so the block's text got both its own tight background *and* the new line's, stacked visibly. Fix: `t.monospace` now only sets text `color`; a second tree-walking plugin, `inlineCodeBackground`, gives `InlineCode` nodes specifically their own tight `Decoration.mark` chip. `InlineCode` and `FencedCode`/`CodeBlock` never nest inside each other in the grammar, so the two plugins are mutually exclusive by construction — there's no shared code path left that could double them up again.

**Verified** (both fixes, together): `npx tsc --noEmit` and build clean. Live, this time against the scratch `Poco Demo` workspace (the user's own dev server had stopped by then, freeing port 5173) — typed plain text, an inline code span, and a fenced block together, selected the whole thing via real keyboard events (`ctrl+Home`/`ctrl+shift+End`, not a synthetic DOM `Selection` object, which doesn't drive CodeMirror's own selection state), and read every rendered `.cm-selectionBackground` rectangle's computed `background-color` directly — all resolved to `--accent-soft`'s dark-theme value with none hidden, and switching back to light theme confirmed the light value too.

## Follow-up: selection and text both light in dark theme

Still visibly wrong even after the translucency fix above: "in dark theme, the text and selected text highlight are both light color, make them hard to read." Root cause was different from the previous selection bug — `drawSelection()` (used so the selection is themeable at all) suppresses the browser's *native* `::selection`, which this app already themes everywhere via `--accent-soft`, and substitutes CodeMirror's *own* selection layer, styled by CodeMirror's own injected stylesheet: hardcoded light-grey (`#d9d9d9` unfocused, `#d7d4f0` focused), with zero awareness of this app's `data-theme` attribute. Dark theme's near-white `--ink` text was landing on a selection background nearly as light as the text itself.

Fix: `.md-editor .cm-selectionBackground { background: var(--accent-soft) !important; }` — reuses the exact token the rest of the app's native `::selection` already uses, so CodeMirror's selection now matches everywhere else in both themes. `!important` specifically (a first for `app.css` — checked, no prior use) because CodeMirror injects its stylesheet at runtime, after `app.css` loads, with several chained classes on its own selectors; normal specificity/source-order isn't reliable against a JS-injected third-party stylesheet with unpredictable load timing.

**Verified live** against the scratch `Poco Demo` workspace (the user's dev server had stopped again by this point, so a real screenshot and full interaction test were possible, not just computed-style reads): selected text via real keyboard input, confirmed all three rendered `.cm-selectionBackground` rectangles resolved to `oklch(0.3 0.06 45)` (dark theme's `--accent-soft`) with `getComputedStyle`, and confirmed light theme still resolved to its own light `--accent-soft` with no regression. `npx tsc --noEmit` and the build clean throughout.

## Follow-up: made the autosave interval a user preference, not a fixed constant

The user asked for a Settings option to configure the autosave period, defaulting to 30s (superseding this milestone's earlier hardcoded 4000ms constant).

- **Server**: `autosaveIntervalSeconds?: number` added to `WorkspaceRegistry` (`server/src/lib/workspaces.ts`), same app-wide-not-per-workspace shape as `theme`/`calendarMode`. `services/workspaces.ts` gained `get/setAutosaveIntervalPreference` (`undefined` normalizes to `30`) plus exported `AUTOSAVE_INTERVAL_MIN_SECONDS`/`AUTOSAVE_INTERVAL_MAX_SECONDS` constants (`1`/`300`) so the route's validation bounds and the preference's own documented range can't drift apart. `routes/preferences.ts` gained `GET/PUT /api/preferences/autosave-interval`, rejecting anything that isn't an integer in that range with a 400. Two new tests in `server/src/services/workspaces.test.ts` (115 total, up from 114).
- **Client**: `api/client.ts` gained the matching `get/setAutosaveIntervalPreference`. `SettingsPage.tsx` gained an **Autosave** section — a number input, seconds, using the same draft-state + validate-on-blur pattern the Sync panel's remote-URL field already established (not a radiogroup like language/theme, since this is a free-form bounded number, not a fixed set of options) — with client-side range/integer validation before ever hitting the API, since a bare `<input type="number">` doesn't enforce that on its own. `JournalDayPage.tsx` replaced its hardcoded `AUTOSAVE_DELAY_MS` constant with its own `useQuery` for this preference (not bootstrapped app-wide in `main.tsx` the way theme/language are — this only affects a debounce timer, never anything rendered before the journal page exists), held in a ref so the debounce's `setTimeout` always reads the current value without the scheduling function needing to be redefined every time the query resolves. `en.json`/`vi.json` both gained a `settings.autosave.*` block (245 leaf keys each, up from 240 — confirmed structurally identical via a throwaway key-diff script).
- **Verified**: `npx tsc --noEmit` (server + client) and `npm run build -w client` clean; `npm test -w server` 115/115. The user's own dev server was running throughout this pass but wasn't observed to pick up the new server route via its `tsx watch` — rather than guess why or poke at a process this session didn't start, verification used a fully isolated instance instead (`tsx server/src/index.ts` with `PORT`/`HOME` both overridden to scratch values, so it never touched `~/.poco/config.json` or any real workspace): confirmed the default (`30`), that a valid PUT (`60`) persists and reads back correctly (including surviving a re-read of the actual written `config.json`), and that every invalid shape (`0`, `301`, `5.5`, a string) is rejected with the same structured 400. The scratch instance and its temp `$HOME` were torn down afterward. The Settings page's own UI logic was verified by code review rather than a live click-through (port 5173 was occupied by the user's live session, and its Vite proxy target is hardcoded to the shared server) — it mirrors an already-shipped, working pattern in the same file (the Sync panel's `remoteUrlDraft`/`commitRemoteUrl`) closely enough that this was judged sufficient, but a live click-through of the actual number input is still outstanding.

## Bug: "Saving…" always shown, from a spurious autosave on every page load

The user reported "the text Saving… below editor is always showed." Root cause predates this session's autosave-interval work but only became obviously visible once the debounce grew from 800ms/4000ms to a user-configurable 30s default: `MarkdownEditor`'s "sync an externally-changed `value` prop into the CM doc" effect (needed for the journal entry's fetch resolving after the editor already mounted empty) dispatched a real CodeMirror transaction to push the loaded content in — and the `updateListener` had no way to tell that dispatch apart from the user actually typing, so it called `onChange` for it exactly the same as a real edit. `JournalDayPage`'s autosave logic can't tell "the user typed something" from "the editor's content was just set programmatically," so *every* journal page load scheduled a real (and pointless) autosave — previously self-resolving within a few seconds and going unnoticed, now sitting in "Saving…" for up to 30 seconds on every visit, looking permanently stuck.

Fix: tag the sync effect's `dispatch` with a dedicated CodeMirror `Annotation` (`externalValueSync`), and have the `updateListener` skip calling `onChange` for any transaction carrying it — CodeMirror transactions support arbitrary annotations exactly for this "was this change programmatic or from the user" distinction.

**Verified live** — this time against a *fully* isolated instance, not just a scratch workspace: a separate server (`tsx server/src/index.ts`, scratch `$HOME`/port) and a temporarily-repointed `vite.config.ts` proxy (reverted to the original `4174` immediately after, confirmed via an empty `git diff` on that file) running on a free port, entirely independent of both the user's real vault and their live dev server (which was running throughout, untouched). Pre-seeded a journal entry with existing body content via the API, loaded the journal page, and confirmed the save-status text was empty immediately after load and stayed empty after several seconds (previously would have shown "Saving…" then eventually "Saved" with zero actual edits). Then, with the autosave interval temporarily set to 1s on that same scratch instance, made a real edit and confirmed the full `"" → "Saving…" → "Saved"` cycle still worked correctly. `npx tsc --noEmit` and `npm test -w server` (115/115) clean throughout.

## Follow-up: "Saving…" still showed the whole time while actively typing

The previous fix addressed the spurious autosave-on-load, but the user reported "it still always show saving... when i start typing" — a different, real behavior: the autosave debounce resets on every keystroke, so while actively composing with pauses shorter than the configured interval, the timer never fires at all. `scheduleSave` was labeling that entire waiting stretch "Saving…", which is misleading — nothing has actually started saving yet, it's just pending — and reads as permanently stuck once the interval is tens of seconds long (which it now commonly is, given the previous milestone follow-up's 30s default).

Fix: split the save-status states into `'idle' | 'unsaved' | 'saving' | 'saved'`. `scheduleSave` now sets `'unsaved'` immediately on every edit; `'saving'` is only set right before the debounce timer actually fires and calls `saveMutation.mutate(...)` — i.e. only while the PUT request is genuinely in flight. New `journalDay.unsaved` i18n key ("Unsaved changes" / "Có thay đổi chưa lưu") added to both `en.json`/`vi.json` (246 leaf keys each, confirmed structurally identical).

**Verified live** against another fully isolated instance (same recipe as the previous fix — scratch `$HOME`/port, `vite.config.ts` proxy temporarily repointed and reverted immediately after, confirmed via an empty `git diff`), with the autosave interval set to 3s for a fast test cycle: typed three characters roughly 800ms apart (well under the 3s interval) and confirmed the status stayed "Unsaved changes" after each one, never "Saving…"; then let it sit idle and confirmed it flipped to "Saved" once the debounce actually fired. Also re-confirmed the load-time fix still holds (empty status immediately after navigating to an entry with existing content). `npx tsc --noEmit` and `npm test -w server` (115/115) clean.
