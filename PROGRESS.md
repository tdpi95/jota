# Progress Tracker

Tracks implementation of [PLAN.md](PLAN.md). Update this file as milestones start/finish — check a box only when its own Verify step (from PLAN.md) has actually been run, not just when code is written.

Each milestone's detailed build notes (design decisions, gotchas, "Verified live" narratives) live under [docs/progress-notes/](docs/progress-notes/), one file per milestone (or per milestone group, where several were built in one pass) — linked from each checklist line below. Read this file for status; open a specific note file only when you need that milestone's history (picking it back up, or checking why a decision was made).

## Status: Milestones 1-17, 19 (Localization), 20 (Calendar page), 21 (Theming), 23 (Journal editor syntax highlighting), 24 (Notes), 25 (Search improvement), 26 (Project groups), and 27 (File attachments) done; milestone 18 (Polish) in progress; milestone 22 (Concurrent-write safety) not started, deliberately deferred

## Scaffold done so far
- [x] Root `package.json` (npm workspaces: server, client, electron) + `.gitignore`
- [x] `server/package.json` (Express, gray-matter, date-fns, cors, tsx, typescript; no SQLite npm dep — using `node:sqlite`)
- [x] `server/tsconfig.json` (NodeNext module/resolution, strict)
- [x] `client/` — Vite + React + TS scaffold (placeholder `App.tsx`; real UI is milestones 11-16)
- [x] `electron/` — Electron + electron-builder scaffold (see [Milestone 3 notes](docs/progress-notes/milestone-3.md))
- [x] `design/` — UI prototype canvas (source files + live link), see [design/README.md](design/README.md)
- [ ] Everything else below

## Milestones

- [x] **1. Vault + markdown core v2** — task grammar (description/created/doing-timer tokens) + project/journal frontmatter v2, workspace-path-agnostic. Verify: `npm test -w server` — round-trip parse→serialize on hand-written fixtures (`server/src/lib/markdown/__fixtures__/{project-sample,journal-sample}.md`) is byte-for-byte equal; also `npx tsc --noEmit` clean. See [Milestone 1 notes](docs/progress-notes/milestone-1.md).
- [x] **2. Workspace management** — `~/.poco/config.json` registry, add/list/open/remove, `.poco/` scaffold-on-open. Verify: `npm test -w server` (`server/src/services/workspaces.test.ts`) plus a manual curl run against the live server (see [Milestone 2 notes](docs/progress-notes/milestone-2.md)) registering two scratch folders, switching, and confirming removal doesn't touch the folder.
- [x] **3. Electron shell** — `electron/` workspace, `main.ts` (embedded server + `BrowserWindow`, hide-not-close), `preload.ts` (`pickFolder`, login-item/reminder bridges). Verify: `npm run dev` at root, driven via a Playwright `_electron` script (see [Milestone 3 notes](docs/progress-notes/milestone-3.md)) — window opens showing the placeholder client talking to the embedded server over HTTP, closing it hides rather than destroys the window, and it's still responsive after a simulated tray reopen.
- [x] **4. Indexing layer** — per-workspace `node:sqlite` (WAL mode), reconciliation-based (re)indexing, rebuild/status routes. Verify: `npm test -w server` (`server/src/lib/index/reindex.test.ts`) plus a manual curl run against the live server (see [Milestone 4 notes](docs/progress-notes/milestone-4.md)) — seeded a scratch workspace, rebuilt the index, inspected rows against the files, deleted the sqlite file and confirmed identical reproduction on rebuild.
- [x] **5. Backup/versioning layer** — `lib/vaultGit.ts` (init/commit-on-write/revert), history/diff/revert routes. Verify: `npm test -w server` (`server/src/lib/vaultGit.test.ts`) plus a manual curl run (see [Milestone 5 notes](docs/progress-notes/milestone-5.md)) — committed a project creation and a status change as separate commits, confirmed `git log`/the history route show one commit per change with the `[origin] message` format, reverted the status-change commit and confirmed both the file content and `git log` reflect the undo, and confirmed the index re-reconciles after a revert.
- [x] **6. Remote sync layer** — `lib/sync/{types,gitRemote}.ts`, remote get/set + push/pull/status, conflict surfacing. Verify: `npm test -w server` (`server/src/lib/sync/gitRemote.test.ts`) plus a manual curl run against the live server (see [Milestone 6 notes](docs/progress-notes/milestone-6.md)) — pointed a scratch workspace at a local bare repo, pushed, cloned it as a second workspace, pushed a conflicting edit to the same line from there, and confirmed the first workspace's pull returned a structured conflict list with the working tree left exactly as it was (no conflict markers, no mid-merge state).
- [x] **7. Service layer + Backend Projects+Tasks API v2** — CRUD w/ description/tags/color/createdAt, doing-timer transition, index + git commit on write. Verify: `npm test -w server` (`server/src/services/{projects,tasks}.test.ts`) plus a manual curl run against the live server (see [Milestone 7 notes](docs/progress-notes/milestone-7.md)) — created a project (confirmed color auto-assignment/cycling), added a task, PATCHed todo→doing→todo, and confirmed the file's `@spent`, `GET /api/index/status`, and `git log` all agree on the change.
- [x] **8. Backend Journal API v2** — `linkedTasks`, link add/remove endpoints. Verify: `npm test -w server` (`server/src/services/journal.test.ts`) plus a manual curl run against the live server (see [Milestone 8 notes](docs/progress-notes/milestone-8.md)) — GET/PUT'd an entry, linked/unlinked a task, confirmed the file, `GET /api/journal/:year`, `GET /api/tasks/:taskId/journal-links`, and `git log` all agree after each step.
- [x] **9. Backend aggregate/query routes** — open tasks, calendar marks, journal-links, search, time-spent report. Verify: `npm test -w server` (`server/src/services/{calendar,reports}.test.ts` + aggregate tests appended to `services/tasks.test.ts`) plus a manual curl run against the live server (see [Milestone 9 notes](docs/progress-notes/milestone-9.md)) — cross-checked `/api/tasks/open`, `/api/tasks/search`, `/api/calendar/:year/:month`, and `/api/reports/time-spent` (both `includeInProgress` on and off) by hand against the seeded `.md` files.
- [x] **10. MCP server** — stdio server, `POCO_WORKSPACE` targeting, full read+write tools + `get_task_summary`. Verify: `npm test -w server` (`server/src/mcp/tools.test.ts`, driven through a real `@modelcontextprotocol/sdk` `Client` over `InMemoryTransport`) plus a manual run against the actual stdio subprocess (see [Milestone 10 notes](docs/progress-notes/milestone-10.md)) — created/updated via tool calls and confirmed files/git history matched what the REST API produces, reverted an MCP-made change via `POST /api/vault/revert/:commit` and confirmed it cleanly undid it, and confirmed switching the active workspace via the REST API did not change what the already-running MCP server (targeted via `POCO_WORKSPACE`) operated on.
- [x] **11. Frontend Projects/Tasks UI v2** — description, color picker, tags, live `TimeSpentBadge`. See [Milestone 11 & 12 notes](docs/progress-notes/milestone-11-12.md).
- [x] **12. Frontend Journal UI v2** — linked-tasks section + picker, "+ log to today". See [Milestone 11 & 12 notes](docs/progress-notes/milestone-11-12.md).
- [x] **13. Frontend Calendar sidebar** — month grid in `AppShell`, marks, click-to-navigate. See [Milestone 13 & 14 notes](docs/progress-notes/milestone-13-14.md).
- [x] **14. Frontend Dashboard v2** — color badges, "+ log to today" wired in. See [Milestone 13 & 14 notes](docs/progress-notes/milestone-13-14.md).
- [x] **15. Frontend History & revert UI** — History panel on project/journal pages. See [Milestone 15 & 16 notes](docs/progress-notes/milestone-15-16.md).
- [x] **16. Frontend Workspace switcher + Settings/Sync panel** — native folder picker; reminder time + launch-at-login fields. See [Milestone 15 & 16 notes](docs/progress-notes/milestone-15-16.md).
- [x] **17. Daily reminder** — tray icon, `loginItem.ts`, `reminder.ts` scheduler, native notification. See [Milestone 17 notes](docs/progress-notes/milestone-17.md).
- [ ] **18. Polish** — in progress. Done: task drag-and-drop on the Kanban columns, project create/edit in a `Modal`, a custom-color swatch, a projects tag filter, [README.md](README.md) rewritten to match actual status, auto-refresh after an out-of-band vault change, a git-not-found notice + MCP agent-setup instructions on Settings, an "open workspace folder" button per workspace row, task-checklist (sub-tasks) support end-to-end (grammar, service, REST, MCP, UI), a basic `electron-builder` packaging config producing a working standalone Linux AppImage, a fix for the MCP self-serve config so it generates a correct, runnable command for a packaged install too (not just a dev checkout), then a follow-up fix so that command is also *stable* across AppImage restarts (routes it through `$APPIMAGE` + a `--poco-mcp-server` relaunch flag instead of the ephemeral per-launch mount path), a single-instance lock so double-clicking the AppImage (or any launch icon) more than once focuses the existing window instead of spawning a second server/tray/reminder-scheduler, an AppImage `desktopName`/`syncDesktopName`/`executableName` fix so the taskbar/dock correctly associates the running window with its (already-correct) icon, a Settings toggle to add/remove a `~/.local/share/applications` entry for the running AppImage, and a fix for the packaged-AppImage MCP snippet disconnecting under Claude Desktop (it now also carries `DISPLAY`/`DBUS_SESSION_BUS_ADDRESS`, needed because relaunching the AppImage always boots Electron's native layer first and segfaults without a real display/session-bus connection), and a follow-up so the MCP snippet no longer hardcodes `POCO_WORKSPACE` to whatever workspace happened to be open when it was copied — it defaults to (and now says so) whichever workspace poco currently has open, with `POCO_WORKSPACE` documented as an opt-in for pinning to one workspace specifically, and — direct follow-up to the project create/edit modal — task create/edit also moved into a `Modal` (`TaskRow`'s edit action, shared by the Kanban board and the Dashboard, plus a new full-form create modal reached via the quick-add row's "more fields" button) while the single-line quick-add row itself stays inline exactly as before, and task descriptions switched from a plain unparsed textarea to the same `MarkdownEditor`/`renderMarkdownToHtml` (syntax-highlighted editing, rendered-HTML display) notes and the journal body already use, then a same-session follow-up replacing `TaskRow`'s click-to-expand-in-place with a titled *view* `Modal` (badges, interactive checklist, rendered description, and an Edit button that swaps straight into the edit modal), followed by a further fix/addition so every task row opens that modal (not just ones with a checklist or description) and it now also shows the task's created time, then a later follow-up ("suggest shortcuts for quick add journal/task, opening dashboard, projects, notes, calendar, settings... if user is editing something, show a warning before moving away") adding app-wide keyboard shortcuts: Cmd/Ctrl+1..5 and Cmd/Ctrl+, jump straight to Dashboard/Projects/Notes/Journal/Calendar/Settings (mirrored as hover tooltips on the sidebar's nav items), Cmd/Ctrl+T opens a quick-add-task popup from any page (`QuickAddTaskModal`, pulled out of `DashboardPage` so its own "+ Add task" button and the new shortcut share one implementation instead of two), and Cmd/Ctrl+J jumps to today's journal entry with the editor already focused; a new app-wide "unsaved changes" registry (`lib/unsavedChanges.tsx`) backs the warning — `TaskForm`/`ProjectForm`/`QuickAddTaskModal` (the forms that don't autosave) register while their fields differ from their initial values, and every shortcut-driven navigation confirms first if anything is currently dirty, matching a browser's own "leave site?" prompt, then a further follow-up ("allow find in note and journal using ctrl+F") adding CodeMirror's own built-in find UI (`@codemirror/search`'s `search()` + `searchKeymap`, now a direct dependency) to the shared `MarkdownEditor` — Cmd/Ctrl+F opens a themed find/replace panel pinned above the first line (`top: true`, needed since these editors have no internal scroll of their own to keep a bottom panel on screen past a screenful of content) in both the journal body and note bodies, styled with the app's own `--paper`/`--ink`/`--accent` tokens rather than CodeMirror's default light-grey chrome, then a same-session fix ("it doesn't work when the editor is not focus or in preview mode") after the original relied only on CodeMirror's own keymap binding (which needs the editor itself focused, and doesn't exist at all in Preview mode): `MarkdownEditor` gained an imperative `openFind()` handle, and a new shared `useFindShortcut` hook (page-scoped Cmd/Ctrl+F listener in `JournalDayPage`/`NoteBodyEditor`) switches to Edit mode first if needed and then calls it, working now regardless of current focus or view mode, then a further fix (user-reported, with a screenshot: a match inside a heading rendered as a solid box with no visible text) after the highlight's `--accent`/`--accent-contrast` colors turned out to exactly coincide with a heading/link's own `--md-heading`/`--md-link` (both `= var(--accent)`) — CSS inheritance can't let a parent's `color` override a child's own explicit rule, `!important` or not, so the heading kept its own (now-invisible-against-itself) text color; fixed with new, translucent, fixed-hue `--find-match-bg`/`--find-match-selected-bg` tokens (warm yellow/orange, independent of `--accent-hue` and every `--md-*` token, same "translucent so the underlying color still shows through" fix `--md-code-bg` already established for an unrelated bug) instead of trying to force text color at all, then a further follow-up ("use icons and collapsible replace inputs like in vscode") replacing `@codemirror/search`'s default find/replace panel (a fixed DOM of text buttons + always-visible replace fields, not reshapeable via CSS alone) with a from-scratch one (`lib/vscodeSearchPanel.ts`'s `VscodeSearchPanel`, plugged in via `search()`'s `createPanel` option) reusing all of that package's own query/command machinery: a single search row (chevron · input · live "n of m" count · `Aa`/`ab`/`.*` icon-style toggles for case/whole-word/regex, styled the way VS Code's own widget actually renders them · prev/next chevrons · close ×) with the replace row (input + replace/replace-all icons) collapsed behind that chevron by default, matching VS Code's own interaction — caught and fixed two real bugs along the way (a `hidden`-attribute-losing-to-`display:flex` CSS specificity bug hiding the "collapsed" state incorrectly, and a false alarm where Enter seemed to not commit search navigation, root-caused to a test-tool key-dispatch quirk rather than a real code bug), then a further fix (user-reported, with a screenshot: typing `#` rendered visibly offset/cut off in the find input) after the panel's inherited `font-family: 'JetBrains Mono'` + `line-height: 1.75` (both sized for full lines of editor prose, not a single-line `<input>`) turned out not to reconcile cleanly with the input's own text-box layout — fixed with an explicit UI font (the same one every other input in the app already uses) and `line-height: normal` on the panel instead of inheriting the editor's own values, then a further fix (user-reported: "ctrl+K doesn't work on other pages rather than dashboard") after the "search everything" popup's Cmd/Ctrl+K listener turned out to still be a plain `useEffect` scoped to `DashboardPage`'s own lifetime (predating the milestone's global-shortcuts work) — pulled the whole popup out into a new, fully self-contained `SearchModal` (own data fetch for the tag pills, own task mutations) and wired Cmd/Ctrl+K into `KeyboardShortcuts.tsx` the same way Cmd/Ctrl+T already opens `QuickAddTaskModal` from anywhere; `IndexedTask -> Task` conversion moved to a new shared `lib/tasks.ts` since both `DashboardPage`'s buckets and `SearchModal`'s results need it now (see [Milestone 18 notes](docs/progress-notes/milestone-18.md)). Still outstanding: first-run state, loading/error states, backup job, Windows/macOS installer polish (icons, code signing).
- [x] **19. Localization (i18n)** — started at the user's request ahead of milestone 18's remaining items. `language` preference (registry field + `GET/PUT /api/preferences/language`), `react-i18next` client scaffold (`client/src/i18n/`), `main.tsx` resolving the stored language before first render, a Language picker on `/settings`, every page/component converted to `t()`, `lib/date.ts`'s month/weekday names localized, and the Electron tray/daily-reminder strings (`electron/src/i18n.ts`, wired into `tray.ts`/`reminder.ts`/`main.ts`). Verify: `npx tsc --noEmit` clean (server/client/electron), `npm run build -w client` clean, `npm test -w server` (92 tests) passing, `en.json`/`vi.json` structurally identical (166 leaf keys each). See [Milestone 19 notes](docs/progress-notes/milestone-19.md).
- [x] **20. Frontend Calendar page** — started at the user's request ahead of milestone 18's remaining items, same as milestone 19. A new `/calendar` route (`CalendarPage.tsx`): a full-page month grid listing task titles per day (distinct from `CalendarSidebar`'s compact dots), a due/journal toggle, a journal-entry background highlight (also added to `CalendarSidebar`, replacing its old dot), and a year overview for fast month-picking. Follow-up (same milestone): year-view day cells link to that day's journal entry (previously unclickable), plus a "show all journal entries" toggle button in both granularities that swaps the grid for an inline list of entries (same page, not a modal, and not shown alongside the grid — an either/or toggle) with body preview/expand, backed by a new `GET /api/journal/:year/full`; the due/journal mode toggle and the month/year granularity toggle now both persist across restarts as new `calendarMode`/`calendarGranularity` app-wide preferences (`~/.poco/config.json` + `GET/PUT /api/preferences/{calendar-mode,calendar-granularity}`), same pattern as `theme`/`accentPalette`. See [Milestone 20 notes](docs/progress-notes/milestone-20.md).
- [x] **21. Theming: color palettes + dark theme** — a new milestone requested by the user, started ahead of milestone 18's remaining items. `~/.poco/config.json` gains `theme`/`accentPalette` app-wide preferences (`GET/PUT /api/preferences/{theme,accent-palette}`), a Theme (Light/Dark) and Accent color (Default/Green/Blue/Violet) picker on `/settings`, and an `app.css` token refactor: an `--accent-hue` variable palettes override (keeping the original color as `'default'`), new muted-surface/border/overlay/shadow tokens replacing ~20 previously-hardcoded color literals, and a full `:root[data-theme='dark']` override block. Follow-ups (same milestone): a measured contrast bug in the dark theme's "white text on solid accent/good fill" buttons/badges (~2.5:1, fixed to ~6-8:1 via new `--accent/good/danger-contrast` tokens) plus a themed `::selection` rule; then a correction after the user reported the calendar's journal-entry-day highlight was "still green" regardless of palette — it had been wired to the fixed `--good` "success" semantic instead of the accent family, fixed with a new `--accent-border` token so it now tracks the selected palette while `--good` stays reserved for actual done/success indicators. See [Milestone 21 notes](docs/progress-notes/milestone-21.md).
- [x] **23. Journal editor syntax highlighting** — a new milestone requested by the user ("add markdown preview for journal, live on editor similar to Obsidian"), started ahead of milestone 18's remaining items, same as 19-21. After clarifying scope (Obsidian-style hide-the-marks live preview vs. VS Code-style source coloring), the user chose the latter: a new `MarkdownEditor.tsx` (CodeMirror 6 + `@codemirror/lang-markdown`, GFM-extended) replaces the plain textarea for the journal body only (task/project descriptions keep `MarkdownTextarea`), coloring headings/bold/italic/strikethrough/links/inline-code/blockquotes live as you type — the raw markdown text is unchanged, nothing is rendered as rich HTML or hidden. Several follow-ups landed in the same milestone: a longer autosave debounce (fewer git commits while journaling) later turned into a user-configurable `autosaveIntervalSeconds` preference (`GET/PUT /api/preferences/autosave-interval`, `1`-`300`s, default `30`, a matching Autosave field on `/settings`), a monospace font with accent-colored headings, JetBrains Mono embedded and swapped in app-wide (replacing IBM Plex Mono), a full-width code-block background (its own decoration layer, not a HighlightStyle color), and a themed text-selection color matching the rest of the app in both themes — plus the bugs each of those surfaced and fixed along the way (see the notes file for the full list). New `--md-*` theme tokens in `app.css` (light + dark) keep it consistent with the existing theme/accent-palette system. Verify: `npx tsc --noEmit` (server + client) clean, `npm run build -w client` clean, `npm test -w server` (115/115) throughout; verified live at each step against either a scratch workspace (`Poco Demo`) or a fully isolated server instance (scratch `$HOME`/port), never the user's real vault or their own running dev server — see [Milestone 23 notes](docs/progress-notes/milestone-23.md) for the full narrative, every bug found, and the `styleTags` recursion gotcha. See also [PLAN.md's "Journal editor"](PLAN.md#journal-editor) section for the settled design.
- [ ] **22. Concurrent-write safety (multiple writers, same project file)** — not started, deliberately deferred at the user's request ("we will resolve it later") after they asked whether it's safe for multiple agents, or a human in the UI and an agent, to write at the same time. It isn't, fully: `saveProjectFile` has no locking/optimistic-concurrency check (two processes racing its read-modify-write can silently clobber each other's entire project file, not just a conflicting field), and separately, `TaskForm.tsx`'s edit form resubmits its whole field bundle rather than a diff, so an agent's change to a task can be silently reverted by a human's already-open edit form even without a literal race. See PLAN.md's milestone 22 for the full problem writeup and the two (undecided) candidate fixes.
- [x] **24. Notes** — a new content type requested by the user ("just a collection of md files with some meta: tag, create at, last modified"), checked against the project's storage/indexing/MCP conventions before building (a new feature not previously in PLAN.md, per CLAUDE.md's working conventions). `<workspace>/notes/<slug>.md`, frontmatter `{title, created, updated, tags}`, slug fixed at creation (auto-suffixed on a colliding title rather than rejected, unlike a project); a new `notes` SQLite index table (metadata only, never body — same as journal entries); `GET/POST /api/notes` + `GET/PATCH/DELETE /api/notes/:slug`; five new MCP tools (`create_note`/`update_note`/`delete_note`/`get_note`/`list_notes`) at full parity from the start; `/notes` + `/notes/:slug` frontend pages, reusing `JournalDayPage`'s autosave-editor pattern for title+tags+body. Also fixed a latent bug this surfaced: `frontmatter.ts`'s unquoted-YAML-date coercion would have silently truncated a hand-edited note timestamp's time-of-day; now only an exact-UTC-midnight `Date` is treated as date-only. Follow-up (same milestone, "add md preview for note body"): an Edit/Preview toggle on the body, rendering real HTML via `markdown-it` (`html: false` so a note body — MCP-writable, sync-editable — can't inject a live `<script>`/raw HTML tag through the preview), styled with the same `--md-*` tokens the editor's syntax coloring already uses; then icon-only + right-aligned per follow-up feedback. Follow-up: a "← All notes" back-link on `NoteDetailPage`. Follow-up ("allow edit note file name... I don't want they always be untitled-note-x"): `PATCH /api/notes/:slug` and `update_note` gained an optional `newSlug` — an explicit, click-to-edit rename (409s on collision rather than auto-suffixing, unlike creation) that moves the file, stages both paths in one commit, and navigates the page to the new URL on success. Follow-up ("don't create new note file before user edit anything"): "+ New note" now navigates to a new `/notes/new` draft page (`NewNotePage.tsx`) instead of calling `create_note` immediately — nothing is written to disk/git/index until the user's first real edit there, same lazy-creation principle journal entries already use (`getJournalEntry`'s in-memory default). See [Milestone 24 notes](docs/progress-notes/milestone-24.md) and [PLAN.md's "Notes"](PLAN.md#note-file--one-per-note-workspacenotesslugmd) section.
- [x] **25. Search improvement** — requested by the user ("search improvement. cache journal, task body, not just meta. allow search everything. add search by time range for mcp") after a design discussion about the limits of the current MCP search tools (title/tag-only note search, no journal body search, no cross-type search, no date range on any text search). Added a `body` column to `journal_entries`/`notes` in the index (task `description` was cached already), FTS5 virtual tables (replacing the plain `LIKE`-scan pattern for this one cross-type query), a new `queryFullTextSearch` in `lib/index/queries.ts`, a `services/search.ts`, `GET /api/search?q=&from=&to=&types=`, and a new `search_everything({query, dateRange?, types?})` MCP tool — the first tool that combines free text with a date range. A migration gotcha was caught and fixed before shipping: a bare `ALTER TABLE ... DEFAULT ''` would have left an *unchanged* existing note/journal file's body empty forever (reconciliation skips unchanged mtimes), fixed by also resetting `source_mtime` to force a one-time reparse. Follow-up (same milestone, user: "search both project name/description"): project name/description are now searchable too (`projects_fts`, a fourth `SearchContentType`) — its own migration gotcha, since `projects`' columns didn't change (only its FTS mirror is new): detected via a `sqlite_master` existence check rather than an `ALTER TABLE`-throws check, same mtime-reset backfill once detected. See [PLAN.md's "Search (cross-type full-text)"](PLAN.md#search-cross-type-full-text--milestone-25) and [Milestone 25 notes](docs/progress-notes/milestone-25.md) for the full design, both migration gotchas, and the live verification. Verify: `npm test -w server` (149 tests, up from 138), `npx tsc --noEmit` clean (server/client/electron); verified live against a fully isolated scratch server instance (scratch `$HOME`/workspace/port, never the user's real vault) — a phrase living only in a task description, a note body, two journal bodies, and a project description all matched via `GET /api/search`, date-range and `types` narrowing both worked, and deleting the index and rebuilding reproduced byte-for-byte identical results. Follow-up (same milestone, user asked whether it was wired to the Dashboard; given three options — upgrade the popup, leave it task-only, or something else — chose "upgrade"): the Dashboard's search popup (`DashboardPage.tsx`) now calls the cross-type `GET /api/search` instead of the old task-only `GET /api/tasks/search`, with per-type row rendering — a task result still renders via the existing `TaskRow`; a note/journal/project result gets a new compact row (type badge, title/date, `snippet()` excerpt with the match rendered as `<mark>`, a "go to" link). New `client/src/api/client.ts`'s `search()`, matching `SearchResult`/`SearchContentType`/`IndexedProject` types added to `client/src/types.ts`, new `.search-result-*` CSS reusing `.task-row`'s existing look, and new/renamed i18n keys (`dashboard.searchEverything`/`searchResultType.*`/`searchResultGoTo`/etc., `en.json`/`vi.json` still structurally identical at 268 leaf keys each). Verify: `npx tsc --noEmit` and `npm run build -w client` both clean; verified live via the Browser pane against a second isolated scratch server + a throwaway scratch Vite config on a spare port (deleted after use, confirmed absent from `git status`) — searched a term matching a task, a note, a project, and a journal entry all at once, confirmed each rendered with the right badge/title/snippet and no console errors, clicked all three non-task "go to" links and confirmed each navigated to the right page, and confirmed the popup and its badges/highlights render correctly in both light and dark theme. Follow-up (same milestone, a real bug the user hit): searching their actual "Everything" workspace for an IP address that genuinely existed in a note returned nothing. Root cause: `reconcileWorkspace` had only ever been called from *write* paths — no code called it when a workspace was opened/switched, nor at process boot for whatever was already active, despite PLAN.md documenting exactly that ("runs whenever a workspace is opened, not just process boot") from the start. So a workspace already open before this milestone's schema/cache change (or any future one) never got its existing content backfilled unless something happened to write through it. Confirmed live against the user's real running server (`GET /api/search` returned `[]`; `POST /api/index/rebuild` immediately fixed it — safe and reversible, since the index is only ever a derived cache), then fixed the actual gap: `services/workspaces.ts`'s `addWorkspace`/`openWorkspace` and `index.ts`'s `startServer` (for whatever workspace is already active) now call `reconcileWorkspace`, best-effort, matching every write path's own convention. See [Milestone 25 notes](docs/progress-notes/milestone-25.md) for the full incident writeup. Verify: `npm test -w server` (151 tests, up from 149 — two new `workspaces.test.ts` cases proving a file dropped directly onto disk gets indexed by a plain `addWorkspace`/`openWorkspace` call, no explicit reconcile needed); verified live against a fresh scratch server seeded to reproduce the exact bug (an already-active workspace, content on disk, no `.poco/cache` yet) — a plain server start now indexes it immediately, confirmed via `GET /api/search` with zero explicit rebuild call. Follow-up (same milestone, another real bug: "it only search whole word... if I input 'zago' it should show result that contain the text like 'zagoo'"): `toFtsMatchQuery` was quoting every token as an exact literal phrase, which FTS5 matches as that exact token, not a prefix. Fixed by moving the prefix `*` outside the closing quote (`"zago"*`, not `"zago*"` — confirmed empirically which one FTS5 actually treats as a prefix query rather than assumed) — a whole-word query still matches (trivially its own prefix), so no regression. See [Milestone 25 notes](docs/progress-notes/milestone-25.md) for the full writeup. Verify: `npm test -w server` (152 tests, up from 151); verified live against both the user's real running server (the previous IP-address search still returns its result, and a truncated prefix of it now also matches) and a fresh scratch server (a note containing "Zagoo" is now found by searching "zago"). Follow-up (same milestone, user: "move search button to top near other quick buttons and use ctr/cmd+k shortcut"): the search button moved from beside the task buckets into the page header's `dashboard-header-actions` row (now the first of the three quick actions, alongside "Open today's journal" and "+ Add task"), and a global Cmd/Ctrl+K keydown listener on the Dashboard now opens the popup from anywhere on the page — the same convention VS Code/Slack/Notion/Linear/GitHub all use, with a platform-aware `⌘K`/`Ctrl+K` tooltip hint. The now-empty `.tasks-section-header` CSS rule was removed. Verify: `npx tsc --noEmit` and `npm run build -w client` both clean; verified live via the Browser pane (a tracked `preview_start` dev server this time, after an earlier orphaned scratch dev-server process from prior verification passes in this session turned out to still be live-reloading its old config file and stealing whichever port got written into it — worked around by using a distinct config filename) — the button now renders first in the header row, Ctrl+K opens the popup and autofocuses its input, and Escape closes it.

- [x] **26. Project groups** — a new milestone requested by the user: every project belongs to exactly one group (free-text combobox, not a managed list; blank falls back to "Default"), tasks inherit their project's group purely at query time (no field of their own), a new group filter on the Dashboard, `/projects` re-organized into group-sectioned lists with its own group filter replacing the old project-tag filter, and project `tags` removed outright. See [PLAN.md's "Project file"](PLAN.md#project-file--one-per-project-workspaceprojectsslugmd) section's "Group" subsection and [Milestone 26 notes](docs/progress-notes/milestone-26.md).
- [x] **27. File attachments** — a new milestone requested by the user ("add new milestone: file attachment. allow user to attach files, images to journal, task and note. user can also use a profile image for project"), with four design forks confirmed via `AskUserQuestion` before building (inline markdown embed vs. a structured field, flat vs. per-type attachments folder, MCP tool this pass or not, size cap or not). `<workspace>/attachments/{tasks,journal,notes,projects}/`, a new upload/serve route pair with no SQLite index table, a task/journal/note attachment embedded as a genuine file-relative markdown link (no server-side change needed to those write paths — body/description were already opaque text), a project's profile image as the one structured frontmatter exception, and a new shared `AttachmentField` component (attach button + live thumbnail/chip list) closing a real gap for the journal editor, which has no rendered-HTML view by design. Follow-up (same milestone, "allow paste from clipboard"): pasting a file directly into the journal/note/task editor uploads and embeds it the same way clicking the attach button does — `MarkdownEditor` gained an `onPasteFiles` prop (a CodeMirror `domEventHandlers` paste hook, only intercepting a paste that actually carries files), and the upload/insert logic was pulled out of `AttachmentField` into a shared `useAttachmentField` hook so both entry points use one implementation. Follow-up (same milestone, "open image, file by system apps when click on image, file link"): clicking an attachment now opens it with the OS's own default app (image viewer, PDF reader, ...) instead of a browser tab — needed no client-side change, since Electron's existing `setWindowOpenHandler` (`electron/src/main.ts`) already intercepts every `target="_blank"` link app-wide; it now resolves an `/api/attachments/...` URL to the real file on disk and hands it to `shell.openPath` (the same primitive `openWorkspaceFolder` already uses), with strict traversal-safe path-segment validation (a real bypass in the first draft's guard was found and fixed via isolated logic testing before shipping). Fix (same milestone, "image and file link in detail modal are unclickable"): an embedded image (`![alt](...)`) rendered as a bare `<img>` with no surrounding `<a>` at all — unlike a plain `[text](...)` link, there was genuinely nothing to click — fixed by having `renderMarkdown.ts`'s `image` rule also wrap an attachment image in a real link. Fix (same milestone, "i still can't click on non-image file linke"): the previous system-app-open follow-up turned out not to actually work for a file Electron can't render inline (a PDF, since it has no PDF viewer plugin) — Chromium silently downloads such a file instead of firing the new-window request `setWindowOpenHandler` listens for, so that fix only ever worked for images. Real fix: the renderer now intercepts the click itself (before Chromium can decide to download it) and calls a new `poco:open-attachment` IPC bridge directly, verified live by mocking the bridge and confirming the click is fully intercepted with the correct path and no navigation occurs. Fix (same milestone, "i mean file link below image is unclickable"): a filename containing a space produced a link that couldn't even be parsed — `attachmentMarkdown()` wrote a *bare* CommonMark link destination, which can't legally contain a literal space (or unbalanced parens), so `markdown-it` correctly rendered the literal `[text](...)` text instead of a link, confirmed directly against `markdown-it` before touching code. Fixed by always wrapping the destination in `<...>` (CommonMark's angle-bracket form, which allows any character a filename can actually contain); `markdown-it` auto-percent-encodes the space in the rendered `href` with no changes needed elsewhere. `parseAttachmentRefs` was extended to recognize both the new bracketed form and the old bare one for backward compatibility. Verified live end-to-end by driving the real upload UI (a synthetic file-input `change` event with the exact reported filename) through to a rendered, clickable link. See [PLAN.md's "File attachments — milestone 27"](PLAN.md#file-attachments--milestone-27) and [Milestone 27 notes](docs/progress-notes/milestone-27.md).

## Key decisions log
(Full rationale is in PLAN.md; this is a quick index so a future session doesn't re-litigate them.)

- **Storage**: markdown files are the sole source of truth; SQLite is a derived, always-rebuildable cache — never required to interpret vault content.
- **SQLite library**: `node:sqlite` (built into Node ≥22.5), not `better-sqlite3` (native module, Electron-rebuild friction) or `sql.js` (no real file-backed multi-process concurrency — ruled out because the server + MCP both need concurrent access via WAL mode).
- **Indexing**: reconciliation (stat-diff, parse only changed files) on workspace open, not a blind full reparse — keeps repeat startup fast regardless of vault size.
- **Agent access**: MCP server with full read+write, not a custom in-app chat/OpenAI-compatible feature — any MCP host supplies the agent loop for free.
- **Undo safety net for agent edits**: every workspace is its own git repo, auto-committed on every write (tagged by origin: `[api]` vs `[mcp:<tool>]`), with revert/history routes — independent of whatever approval flow the connecting MCP host does or doesn't have.
- **Multi-workspace**: Obsidian-style — any folder can be a workspace, app-level registry at `~/.poco/config.json`, one active workspace per running app instance, MCP targets an explicit workspace independent of what's open in the app.
- **Remote sync**: git push/pull to a user-supplied remote, no credential management (shells out to the user's own git setup); conflicts are surfaced, never auto-merged. Built behind a `SyncProvider` interface so Google Drive/WebDAV could be added later without a redesign — though passive folder-sync via those tools already works today with zero extra code.
- **Desktop shell**: Electron, not a plain browser-opened web app (direction change) — a thin wrap: the main process launches the existing Express server locally and points a `BrowserWindow` at it, no IPC rewrite of the REST API. Chosen specifically to get a native folder picker and a reliable daily reminder (native OS notification), neither of which a browser tab can do. The main process **spawns** the server as its own process (not an in-process import) — see [Milestone 3 notes](docs/progress-notes/milestone-3.md) for why.
- **Daily reminder**: tray-resident app (window close hides, doesn't quit) with start-at-login **on by default but user-toggleable**; per-workspace reminder time in `~/.poco/config.json`; a main-process scheduler fires a native notification only if today's journal entry is still empty, once per day.
- **Indexing (milestone 4)**: reconciliation is a pure `mtime` stat-diff, not a content-hash compare — a file touched without content changes still gets reparsed. `source_hash` is stored but not load-bearing for staleness today. Non-blocking startup reconciliation is deliberately *not* wired into workspace open yet — no consumer needs it until the aggregate routes (milestone 9) exist.
- **Git identity/history (milestone 5)**: workspaces get a real initial commit (`ensureGitHistory` in `vaultGit.ts`, called from `services/workspaces.ts` in place of the bare `ensureGitRepo`) and a local git identity fallback if the user has no global one configured — both needed for `commitChange`/`revertCommit` to work reliably in a fresh environment, not just conceptually.
- **Frontmatter date normalization**: `lib/markdown/frontmatter.ts`'s `parseFrontmatter` recursively coerces any js-yaml-parsed `Date` back to `YYYY-MM-DD` — an unquoted date scalar (`created: 2026-09-10`, no quotes) is exactly how a human hand-edits the file and otherwise breaks anything downstream expecting a string (found because it broke SQLite parameter binding in milestone 4).
- **Structured service errors (milestone 7)**: `lib/httpError.ts`'s `HttpError` is now the one base class every service module's structured error extends (`WorkspaceServiceError`, `ProjectServiceError`, `TaskServiceError`, `SyncServiceError`) — `index.ts`'s error handler checks `instanceof HttpError` generically instead of importing and special-casing each one.
- **Conflict-safe git pull (milestone 6)**: `lib/sync/gitRemote.ts`'s `pull` never lets a conflicted merge land on disk — it stages a merge with `--no-commit`, inspects `git status --porcelain` for conflict markers, and `git merge --abort`s if any are found, returning the conflicted file list as plain data (not a thrown error) rather than attempting to auto-resolve. `push`/`pull` require a remote already configured via `setRemote`; no credential management, shells out to the user's own git.
- **Task write path reuses the project file I/O (milestone 7)**: `services/tasks.ts` has no filesystem code of its own — `services/projects.ts` exports `loadProjectFile`/`saveProjectFile` (the parse and serialize-write-reindex-commit halves of PLAN.md's write-path order) specifically so task CRUD reuses them, since a task's only storage is as a block inside its project's file. Project color auto-assignment cycles through a fixed 12-color palette by *current project count at creation time* (no separate persistent counter).
- **Journal GET auto-defaults instead of 404ing (milestone 8)**: a `GET` for a date with no file yet returns an in-memory default entry rather than an error — nothing is persisted until an actual write, so opening/browsing dates never creates empty files or git commits. `linkTask` auto-creates the file (PLAN.md, explicit); `unlinkTask` never does (nothing to remove from a file that doesn't exist).
- **Aggregate queries are isolated to `lib/index/queries.ts` (milestone 9)**: all direct `node:sqlite` access for read-only aggregate views (open tasks, search, calendar, time-spent) lives in this one file, matching `db.ts`/`reindex.ts`'s existing isolation of the still-experimental API — `services/{tasks,calendar,reports}.ts` call into it rather than opening `DatabaseSync` connections themselves. Time-spent reporting does its date-filter/group/live-elapsed math in JS over the full task set rather than in SQL, since the file format only stores one cumulative `@spent` total per task (no per-session log to group by day in the database).
- **MCP workspace targeting is resolved once, at process startup (milestone 10)**: `mcp/index.ts` reads `POCO_WORKSPACE` (or falls back to the app registry's active workspace) exactly once and threads that fixed path through every tool call for the process's whole lifetime — an active-workspace switch made later through the REST API/app UI never reaches an already-running MCP server. Verified live, not just asserted. Every MCP tool is tagged `mcp:<tool_name>` as its git commit origin, and errors are returned as structured `isError: true` tool results (never thrown) so the calling agent can read and self-correct rather than hitting a raw protocol failure.
- **Theme/accent-palette storage mirrors `language`, minus the Electron side (milestone 21)**: `theme`/`accentPalette` live on the same app-wide `~/.poco/config.json` registry as `language`/`launchAtLogin`, read/written directly over HTTP with no preload-bridge involvement — but unlike `language`, nothing in `electron/src/` ever reads them, since neither the tray icon nor OS notifications are styled by this app's CSS. `app.css` uses one `--accent-hue` custom property that `[data-palette]` overrides (not a literal hue per palette) so all four palettes share the same lightness/chroma feel; `'default'`'s hue (45) is exactly the app's original, pre-milestone accent color, kept as the default specifically so upgrading doesn't change an existing install's appearance. `:root[data-theme='dark']` overrides every neutral/semantic token in one block, still deferring to `var(--accent-hue)` for anything accent-colored, so theme and palette are independent axes rather than 8 hardcoded combinations.
- **Notes' slug auto-suffixes on a colliding title instead of rejecting (milestone 24)**: deliberately unlike a project's duplicate-name rejection — a note's title ("Meeting notes", "Ideas") is far more likely to be reused verbatim than a project's, and the slug is filename-only, never shown as the note's displayed identity (that's `frontmatter.title`, freely editable afterward without ever touching the filename). The index never caches note body text, matching journal entries' existing rule for the same reason — `GET /api/notes`/`list_notes` and search are metadata-only (title/tags), and only a direct file read (`getNote`) ever sees the body.
- **Frontmatter timestamp fix (milestone 24)**: `lib/markdown/frontmatter.ts`'s unquoted-YAML-date coercion (added in milestone 4 for bare `YYYY-MM-DD` fields) turned out to apply to *any* YAML-timestamp-shaped scalar, including a full ISO8601 datetime — `NoteFrontmatter.created`/`updated` needed one, and the original fix would have silently truncated a hand-edited, unquoted value's time-of-day down to just its date. Now only a coerced `Date` landing exactly on UTC midnight is treated as date-only (the only value a bare-date field could ever produce); anything else keeps its full ISO string. App-written files were never at risk either way — gray-matter/js-yaml already auto-quotes a string that would otherwise round-trip as a `Date`.
- **File attachments are embedded links, not a structured field (milestone 27)**: a task/journal/note attachment is a plain file-relative markdown image/link (`../attachments/<folder>/<file>`) appended into that field's existing body/description text — never a separate `attachments: [...]` array, frontmatter field, or task-line token. This was a deliberate, user-confirmed choice over a structured list specifically so it needed zero server-side change to the task/note/journal write paths (`description`/`body` were already opaque freeform text) and so the reference stays a genuine working relative link in any external tool pointed at the same workspace, not just this app. `<workspace>/attachments/{tasks,journal,notes,projects}/` is real content (not `.poco/`, committed to git like everything else) with no SQLite index table at all — an attachment has no frontmatter/structured fields to parse and cache, so there's nothing for the index's "derived cache over parsed content" job to do here. A project's `profileImage` is the one structured exception (a frontmatter field, since a project has no freeform body to embed into), edit-mode-only, following the exact `undefined`-leaves-alone/`null`-clears convention `UpdateTaskInput.due` already established.
- **Project groups replace project tags outright (milestone 26)**: `ProjectFrontmatter.tags` is gone, not deprecated-alongside — every project now carries exactly one `group: string` instead, normalized to `DEFAULT_GROUP` ("Default") on parse whenever blank/absent (`lib/markdown/project.ts`). A task has no group of its own; `IndexedTask.projectGroup` is joined in at query time only (`lib/index/queries.ts`'s shared `TASK_JOIN_SELECT`), so it's never redundantly stored on a task line. The index's now-pointless `projects.tags`/`projects_fts.tags` columns are kept (always written empty) rather than dropped, matching this file's existing "never require a destructive index migration" caution — an unused column in a derived, always-rebuildable cache costs nothing.
