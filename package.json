{
  "name": "poco",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "client",
    "electron"
  ],
  "scripts": {
    "dev": "concurrently -n client,electron -c green,magenta -k \"npm run dev -w client\" \"wait-on http://localhost:5173 && npm run dev -w electron\"",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wait-on": "^9.1.0"
  }
}
